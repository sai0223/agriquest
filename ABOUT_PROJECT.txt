AgriQuest - Project Activity Summary

Overview:
- Project scaffold: created a Spring Boot backend in `agriquest-backend` (Maven).
- App entry: `AgriquestBackendApplication` with `HealthController` exposing GET /api/health.

Dependencies & Config:
- Added dependencies: Spring Web, Spring Data JPA, PostgreSQL driver, H2.
- Config files: `application.properties` (Postgres defaults), `application-dev.properties` (H2 in-memory), test resources configured for H2.
- Database migration: `db/migration/V1__init.sql` added.

Build & Run:
- Ensured Oracle JDK 21 is active in the terminal for builds/runs.
- Built a repackaged jar: `target/agriquest-backend-0.0.1-SNAPSHOT.jar`.
- Updated `pom.xml` to make H2 available at runtime for dev profile.
- Ran the jar with `--spring.profiles.active=dev` (H2); server started on port 8080.
- Verified health endpoint responded with {"service":"AgriQuest Backend","status":"UP"}.

Troubleshooting & Cleanup:
- Resolved test failures by adding H2 test/dev profiles to avoid needing Postgres during development.
- Removed a redundant `backend` directory containing build artifacts to avoid confusion.

Next recommended steps:
- Commit the removal of the `backend` directory and the current changes.
- Implement domain entities (User, Role, SimulationRun), repositories, and a basic Simulation API slice.
- Optionally configure real PostgreSQL credentials for production runs.

Files/folders of interest:
- agriquest-backend/
- agriquest-backend/pom.xml
- agriquest-backend/src/main/java/com/agriquest/agriquest_backend/AgriquestBackendApplication.java
- agriquest-backend/src/main/java/com/agriquest/agriquest_backend/controller/HealthController.java
- agriquest-backend/src/main/resources/application.properties
- agriquest-backend/src/main/resources/application-dev.properties
- agriquest-backend/src/test/resources/application.properties
- agriquest-backend/src/main/resources/db/migration/V1__init.sql

If you want, I can commit these changes to git now or start adding the initial domain model and API endpoints.